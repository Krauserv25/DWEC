/*
	DESCRIPCION: SCRIPT QUE TRATA LAS VARIABLES GLOBALES
	AUTOR: IVAN VALERA
*/

var config = 
{
	debug: false, //Varaible global que indica si está activo el modo debug o no
	currentID: "", //Variable que indica la id actual (escrita por el usuario) para trabajar en la web
	currentTagmode: "all" //Variable global que indica qué modo de filtro para los tags está señalado
};